apiVersion: v1
kind: Pod
metadata:
  name: log-generator
spec:
  containers:
  - name: logger
    image: busybox
    command: ["/bin/sh", "-c", "while true; do echo \"Application running at $(date)\"; sleep 5; done"]